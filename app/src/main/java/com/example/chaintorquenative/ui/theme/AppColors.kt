package com.example.chaintorquenative.ui.theme

import androidx.compose.ui.graphics.Color

// ─── ChainTorque Brand Colors ─────────────────────────────────────────────────
// Single source of truth — import this object instead of re-declaring colors in every screen.

object AppColors {
    val Primary       = Color(0xFF6366F1) // Indigo
    val Secondary     = Color(0xFF8B5CF6) // Purple
    val GradientStart = Color(0xFF1E1B4B) // Dark Indigo
    val GradientEnd   = Color(0xFF0F172A) // Slate 900
    val CardBg        = Color(0xFF1E293B) // Slate 800
    val Success       = Color(0xFF10B981) // Emerald
    val Warning       = Color(0xFFF59E0B) // Amber
}
